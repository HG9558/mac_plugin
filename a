const openInfoDialog = (matchId: number, row: SaleDataAPI.PreSaleList) => {
  addDialog({
    title: t('盘口预览'),
    props: { matchId, matchInfo: row },
    width: '30%',
    draggable: true,
    hideFooter: true,
    contentRenderer: () =>
      h(OpeningPreview, {
        onTest() {
          console.log('index.vue文件 103==============行打印=', val);
        }
      })
    // contentRenderer: () =>
    //   h(OpeningPreview, {
    //     ref: fomrRef
    //   }),
  });
};
