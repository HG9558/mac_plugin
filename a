const openInfoDialog = (matchId: number, row: SaleDataAPI.PreSaleList) => {
  addDialog({
    title: t('盘口预览'),
    props: { matchId, matchInfo: row },
    width: '30%',
    draggable: true,
    hideFooter: true,
    contentRenderer: () =>
      h(OpeningPreview, {
        onTest: val => {
          console.log('index.vue文件 102==============行打印=', val);
        }
      } as any)
  });
};

如何将any替换为正确的ts类型
