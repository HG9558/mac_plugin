/** 文本复制指令（默认双击复制） */
export const enter: Directive = {
  mounted(el: HTMLInputElement, binding: DirectiveBinding) {
    useEventListener(el, 'keyup', (e) => {
      if (e.key === 'Enter') binding.value();
    });
  },
};

  <el-input
          v-model="form.teamId"
          :placeholder="t('球队ID')"
          clearable
          v-enter="onSearch('reload')"
          class="!w-[200px]"
        />

修改一下这个自定义指令，现在还没有点击自己就执行了
